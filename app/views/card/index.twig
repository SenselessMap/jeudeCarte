<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Jeu de carte</title>
  <link rel="stylesheet" href="{{ asset('assets/styles/styles.css') }}" />
</head>
<body>
  <header>
    {% include 'card/nav.twig' %}
  </header>

  <section class="contenu">
    {% if is_logged_in and is_admin %}
      {% include 'card/form.twig' %}
    {% endif %}

    {% if current_action not in ['journal', 'manageCards'] %}
      {% include 'card/collection.twig' %}
    {% endif %}
  </section>
</body>
</html>